class Solution {
    public int solution(String message) {
        int answer = 0;
        
        int eWidth = 2;
        int latWidth;
        int count = message.length();

        latWidth = (eWidth*count);
        answer = latWidth;
        
        
        return answer;
    }
}